# ~/.veye/.env — veye CLI configuration
# Copy to ~/.veye/.env and edit. Values here are only used if the env var is not already set.

# VisualEyes backend URL (default: http://localhost:8080)
VEYE_API_URL=http://localhost:8080
