apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: visual-eyes-k8s-agent
  namespace: kube-system
  labels:
    app: visual-eyes-k8s-agent
spec:
  selector:
    matchLabels:
      app: visual-eyes-k8s-agent
  template:
    metadata:
      labels:
        app: visual-eyes-k8s-agent
    spec:
      serviceAccountName: visual-eyes-agent
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: visual-eyes-k8s
        image: onkar4545/visual-eyes-kube-agent:latest
        imagePullPolicy: Always
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: VISUAL_EYES_CONFIG
          value: "/etc/visual-eyes/config.yaml"
        - name: VISUAL_EYES_LOG_DIR
          value: "/var/log/containers"
        volumeMounts:
        - name: config
          mountPath: /etc/visual-eyes
        - name: kubelet-ssl
          mountPath: /var/lib/kubelet
          readOnly: true
        # Log collection: read container logs from host filesystem
        - name: varlog
          mountPath: /var/log/containers
          readOnly: true
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "192Mi"
            cpu: "200m"
      volumes:
      - name: config
        configMap:
          name: visual-eyes-k8s-config
      - name: kubelet-ssl
        hostPath:
          path: /var/lib/kubelet
      - name: varlog
        hostPath:
          path: /var/log/containers
