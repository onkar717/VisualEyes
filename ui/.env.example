# VisualEyes UI Environment Configuration Example
# Copy this file to .env and adjust values for your UI environment

# Base URL for backend API (must be prefixed with VITE_ for Vite to expose it)
VITE_API_BASE_URL=http://localhost:8080/api

# (Add any other public UI-specific variables here, always prefixed with VITE_)
