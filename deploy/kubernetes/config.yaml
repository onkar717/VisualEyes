apiVersion: v1
kind: ConfigMap
metadata:
  name: visual-eyes-k8s-config
  namespace: kube-system
data:
  # This ConfigMap uses the configuration from /configs/config-k8s.yaml
  # Only overriding the endpoint for production use
  config.yaml: |
    server:
      endpoint: "http://visual-eyes-backend.kube-system.svc.cluster.local:8080/api/kubernetes-metrics" 