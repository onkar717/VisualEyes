apiVersion: v1
kind: Service
metadata:
  name: visual-eyes-backend
  namespace: kube-system
spec:
  selector:
    app: visual-eyes-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP 